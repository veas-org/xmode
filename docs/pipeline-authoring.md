# Pipeline Authoring

Pipelines compose reusable actions into a graph.

Each pipeline defines:

- `required_context`
- `graph.nodes`
- `graph.edges`
- `triggers`
- `permissions`

Use the visual builder to add action nodes and connect them with conditional edges such as `success`, `failed`, or `approved`.
