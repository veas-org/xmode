<% content_for :page_title, "Edit #{@skill.name}" %>
<%= render layout: "shared/side_panel",
      locals: {
        title: "Edit #{@skill.name}",
        eyebrow: "Skill management",
        description: "Keep skill instructions, objective defaults, plan fallback, and schemas aligned with team practice.",
        back_path: skill_path(@skill),
        wide: true
      } do %>
  <%= render "form", skill_definition: @skill_definition %>
<% end %>
