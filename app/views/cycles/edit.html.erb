<% content_for :page_title, "Edit #{@cycle.name}" %>
<%= render layout: "shared/side_panel",
      locals: {
        title: "Edit #{@cycle.name}",
        eyebrow: "Cycle planning",
        description: "Adjust dates, status, and team ownership for the cycle.",
        back_path: cycle_path(@cycle)
      } do %>
  <%= render "form", cycle: @cycle %>
<% end %>
