<% content_for :page_title, "New schedule" %>
<%= render layout: "shared/side_panel",
      locals: {
        title: "New schedule",
        eyebrow: "Automation timing",
        description: "Attach a pipeline to a one-off or recurring trigger.",
        back_path: schedules_path
      } do %>
  <%= render "form", schedule: @schedule %>
<% end %>
