<% content_for :page_title, "Edit #{@pipeline.name}" %>
<%= render layout: "shared/side_panel",
      locals: {
        title: "Edit #{@pipeline.name}",
        eyebrow: "Pipeline catalog",
        description: "Update the graph, required context, and reusable automation contract.",
        back_path: pipeline_path(@pipeline),
        wide: true
      } do %>
  <%= render "form", pipeline_definition: @pipeline_definition %>
<% end %>
