<% content_for :page_title, "Edit #{@project.title}" %>
<%= render layout: "shared/side_panel",
      locals: {
        title: "Edit #{@project.title}",
        eyebrow: "Project setup",
        description: "Keep project metadata, repository linkage, and automation policy current.",
        back_path: project_path(@project),
        wide: true
      } do %>
  <%= render "form", project: @project %>
<% end %>
