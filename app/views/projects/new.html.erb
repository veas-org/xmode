<% content_for :page_title, "New project" %>
<%= render layout: "shared/side_panel",
      locals: {
        title: "New project",
        eyebrow: "Project setup",
        description: "Define the work boundary, repository context, and automation policy for a team.",
        back_path: projects_path,
        wide: true
      } do %>
  <%= render "form", project: @project %>
<% end %>
