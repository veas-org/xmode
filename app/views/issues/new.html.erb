<% content_for :page_title, "New issue" %>
<%= render layout: "shared/side_panel",
      locals: {
        title: "New issue",
        eyebrow: "Create work",
        description: "Capture the objective, context, ownership, and timing in one focused panel.",
        back_path: issues_path(view: "inbox"),
        wide: true
      } do %>
  <%= render "form", issue: @issue %>
<% end %>
